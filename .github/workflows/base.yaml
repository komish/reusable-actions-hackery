on: [push, workflow_dispatch]

jobs:
  example_job:
    runs-on: ubuntu-latest
    name: Testing Action
    steps:
      - name: Run
        uses: komish/reusable-actions-hackery@latest
        with:
          image: quay.io/opdev/simple-demo-operator:latest